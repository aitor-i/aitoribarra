import styled from "styled-components";

const Wrapper = styled.div`
  .about-image {
    height: 40vh;
    overflow: hidden;
  }
  img {
    position: relative;
    width: 100vw;
  }
`;

export default Wrapper;
