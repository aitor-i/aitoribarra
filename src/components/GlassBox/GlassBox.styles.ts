import styled from "styled-components";

const GlassBoxStyles = styled.div`
  background: rgba(255, 255, 255, 0.6);
  border: 1px solid rgba(255, 255, 255, 0.18);
  border-radius: 10px;
  backdrop-filter: blur(2px);
`;

export default GlassBoxStyles;
